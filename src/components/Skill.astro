---
interface Props {
    name: string;
    url?: string;
}

const { name, url } = Astro.props;
---

{
    url ? (
        <a
            href={url}
            class:list={[
                "border-opacity-50 flex items-center rounded-md border-2 border-gray-200 align-middle",
                "hover:border-teal-100 hover:text-teal-100",
            ]}
        >
            <p class="m-2 text-xs font-semibold text-nowrap">{name}</p>
        </a>
    ) : (
        <div
            class={
                "border-opacity-50 flex items-center rounded-md border-2 border-gray-200 align-middle"
            }
        >
            <p class="m-2 text-xs font-semibold text-nowrap">{name}</p>
        </div>
    )
}
